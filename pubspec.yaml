name: my_flutter_web_sa
description: A new Flutter project.

publish_to: 'none' # Remove this line if you want to publish to pub.dev.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"  # تم تحديث نطاق إصدار SDK لدعم Null Safety

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.4  # تأكد من استخدام الإصدار الأخير المتاح


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
